<?php

/**
 * Implements hook_theme().
 */
function hello_ajax_theme($existing, $type, $theme, $path) {
  return array(
    'hello_ajax_add_page' => array(
      'variables' => array('content' => NULL),
      'file' => 'hello_ajax.pages.inc',
    ),
  );
}
